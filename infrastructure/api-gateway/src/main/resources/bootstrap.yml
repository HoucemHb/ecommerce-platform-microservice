# ===========================================
# API GATEWAY - bootstrap.yml
# File: infrastructure/api-gateway/src/main/resources/bootstrap.yml
# ===========================================

spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false

---
spring:
  config:
    activate:
      on-profile: docker
  cloud:
    config:
      uri: http://config-server:8888